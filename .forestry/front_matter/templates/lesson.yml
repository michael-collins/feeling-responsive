---
label: lesson
hide_body: true
fields:
- name: lectures
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: lectures
  label: Lectures
- name: assignments
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: assignments
  label: Assignments
- name: readings
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: readings
  label: Readings
