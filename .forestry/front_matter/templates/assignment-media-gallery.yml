---
label: Assignment Media Gallery
hide_body: true
fields:
- type: text
  hidden: true
  label: block
  name: block
  default: assignment-h-media-gallery
- type: include
  name: column
  label: Column
  template: text-column
- name: image-gallery
  type: field_group_list
  fields:
  - name: gallery_title
    type: text
    config:
      required: false
    label: Gallery Title
  - name: image
    type: field_group
    config: {}
    fields:
    - name: caption
      type: text
      config:
        required: false
      label: Caption
      description: Add a caption. This is very important for web accessibility standards.
    - name: image_url
      type: file
      config:
        maxSize: 2
      label: Image File
      description: Add an image
    - name: enable_caption
      type: boolean
      label: Enable caption
      description: Enable this to display that caption with the image. Otherwise the
        caption will be hidden.
    label: image
    description: A new image
  config:
    min: 
    max: 
    labelField: 
  label: Image Gallery
