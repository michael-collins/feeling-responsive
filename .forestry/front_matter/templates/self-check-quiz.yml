---
label: self-check-quiz
hide_body: true
fields:
- name: self-check-quiz
  type: field_group_list
  fields:
  - name: question
    type: text
    config:
      required: false
    label: Question
  - name: answer
    type: text
    config:
      required: false
    label: Answer
  config:
    min: 
    max: 
    labelField: 
  label: Self-check Quiz
pages:
- _quizzes/test-quiz.md
